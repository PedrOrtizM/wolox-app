import { WxInputDirective } from './wx-input.directive';

describe('WxInputDirective', () => {
  it('should create an instance', () => {
    const directive = new WxInputDirective();
    expect(directive).toBeTruthy();
  });
});
